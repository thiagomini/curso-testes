info:
  name: criar-autor
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/autores"
  body:
    type: json
    data: |-
      {
        "nome": "H.P. Lovecraft",
        "nacionalidade": "Ingles"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
