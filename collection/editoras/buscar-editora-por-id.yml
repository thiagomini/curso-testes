info:
  name: buscar-editora-por-id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/editoras/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
