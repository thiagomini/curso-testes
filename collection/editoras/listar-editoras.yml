info:
  name: listar-editoras
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/editoras"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
